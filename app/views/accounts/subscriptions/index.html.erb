<h1>Subscriptions for <%= @account.name %></h1>

<p>
  <%= link_to "New Subscription", new_account_subscription_path(@account) %>
  |
  <%= link_to "Back to Account", account_path(@account) %>
  |
  <%= link_to "Products", products_path %>
</p>

<% if @subscriptions.any? %>
  <ul>
    <% @subscriptions.each do |subscription| %>
      <li>
        <strong><%= subscription.product.name %></strong>
        — Licenses: <%= subscription.number_of_licenses %>
        — <%= subscription.issued_at.to_date %> → <%= subscription.expires_at.to_date %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No subscriptions yet.</p>
<% end %>


